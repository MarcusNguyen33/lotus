import {sqliteTable,text,integer} from 'drizzle-orm/sqlite-core';
export const products=sqliteTable('products',{id:text('id').primaryKey(),name:text('name').notNull(),brand:text('brand').notNull(),category:text('category').notNull(),price:integer('price').notNull(),image:text('image').notNull(),description:text('description').notNull(),active:integer('active').notNull().default(1),created:text('created').notNull()});
export const orders=sqliteTable('orders',{id:text('id').primaryKey(),code:text('code').notNull().unique(),tokenHash:text('token_hash').notNull(),requestId:text('request_id').notNull().unique(),name:text('name').notNull(),phone:text('phone').notNull(),email:text('email').notNull(),address:text('address').notNull(),note:text('note').notNull(),items:text('items').notNull(),total:integer('total').notNull(),status:text('status').notNull(),shipment:text('shipment').notNull().default(''),created:text('created').notNull()});
